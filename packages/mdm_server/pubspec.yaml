name: mdm_server
description: An new Dart Frog application
version: 1.0.0
publish_to: none

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  dart_frog: ^0.0.1-dev.10
  database_client:
    path: packages/database_client
  dungeon_repository:
    path: packages/dungeon_repository
  mdm_domain: 1.0.0+1
  user_repository:
    path: packages/user_repository

dev_dependencies:
  mocktail: ^0.3.0
  test: ^1.19.2
  very_good_analysis: ^3.0.0
